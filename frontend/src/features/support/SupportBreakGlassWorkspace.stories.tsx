import type { Meta, StoryObj } from "@storybook/react-vite";
import { expect, userEvent, waitFor } from "storybook/test";
import { http, HttpResponse, delay } from "msw";
import MockDate from "mockdate";
import type { components } from "../../api/schema";
import { SupportBreakGlassWorkspace } from "./SupportBreakGlassWorkspace";
const id = "85000000-0000-4000-8000-000000000001";
const supportCase = { caseId: id, state: "IN_PROGRESS", subjectLinks: [{ linkId: id, subjectType: "CUSTOMER", display: { state: "AVAILABLE", label: "김*현" }, subjectId: id }] };
const request: components["schemas"]["BreakGlassResource"] = { requestId: id, caseId: id, subjectLinkId: id, subjectType: "CUSTOMER", subjectId: id, field: "CUSTOMER_PRIMARY_EMAIL", purpose: "PRIVACY_INCIDENT", reasonCode: "PRIVACY_INCIDENT", state: "APPROVAL_PENDING", requestedAt: "2026-09-11T09:00:00Z", expiresAt: null, version: 0 };
let current: components["schemas"]["BreakGlassWorkflowResource"];
const meta = { title: "Patterns/Support/Break glass", component: SupportBreakGlassWorkspace, tags: ["autodocs"], args: { supportCase, initialRequestId: id }, beforeEach() { MockDate.set("2026-09-11T09:04:00Z"); current = { request: { ...request }, allowedActions: ["DECIDE"], canViewRevealedValue: false, postReview: null }; return () => MockDate.reset(); }, parameters: { a11y: { test: "error" }, docs: { description: { component: "긴급 목적에 필요한 한 필드의 별도 승인·1회 열람·독립 사후 검토를 연결합니다. 원문은 저장·재생하지 않으며 화면 이탈·만료·권한 변경 시 지웁니다." }, story: { inline: false, height: "1000px" } }, msw: { handlers: [http.get("/api/v1/support/break-glass-requests/:id/workflow", () => HttpResponse.json(current))] } } } satisfies Meta<typeof SupportBreakGlassWorkspace>;
export default meta; type Story = StoryObj<typeof meta>;
export const RequestField: Story = { args: { initialRequestId: undefined }, play: async ({ canvas, msw }) => { msw.use(http.post("/api/v1/support/cases/:id/break-glass-requests", async ({ request: httpRequest }) => { expect(await httpRequest.json()).toEqual({ subjectLinkId: id, field: "CUSTOMER_PRIMARY_EMAIL", purpose: "PRIVACY_INCIDENT", reasonCode: "PRIVACY_INCIDENT" }); current.allowedActions = []; return HttpResponse.json(request, { status: 201 }); })); await userEvent.selectOptions(canvas.getByLabelText("긴급 열람 필드"), "CUSTOMER_PRIMARY_EMAIL"); await userEvent.selectOptions(canvas.getByLabelText("긴급 열람 사유"), "PRIVACY_INCIDENT"); await userEvent.click(canvas.getByRole("checkbox")); await userEvent.click(canvas.getByRole("button", { name: "긴급 열람 승인 요청" })); await expect(await canvas.findByText("별도 승인 대기")).toBeVisible(); } };
export const SeparateApproval: Story = { play: async ({ canvas, msw }) => { msw.use(http.post("/api/v1/support/break-glass-requests/:id/approvals", async ({ request: httpRequest }) => { expect(await httpRequest.json()).toEqual({ decision: "APPROVE", expectedVersion: 0 }); current = { request: { ...request, state: "ACTIVE", expiresAt: "2026-09-11T09:06:00Z", version: 1 }, allowedActions: [], canViewRevealedValue: false, postReview: null }; return HttpResponse.json(current.request); })); await userEvent.click(await canvas.findByRole("checkbox", { name: "긴급 사유와 최소 필드 범위를 확인했습니다" })); await userEvent.click(canvas.getByRole("button", { name: "긴급 열람 승인" })); await expect(await canvas.findByText("긴급 열람 승인 결정을 기록했습니다")).toBeVisible(); await waitFor(() => expect(canvas.queryByRole("button", { name: "긴급 정보 한 번 열람" })).not.toBeInTheDocument()); } };
const active = () => { current = { request: { ...request, state: "ACTIVE", expiresAt: "2026-09-11T09:06:00Z", version: 1 }, allowedActions: ["REVEAL"], canViewRevealedValue: false, postReview: null }; };
export const RevealAndClear: Story = { beforeEach: active, play: async ({ canvas, msw }) => { msw.use(http.post("/api/v1/support/break-glass-requests/:id/reveals", async ({ request: httpRequest }) => { expect(await httpRequest.json()).toEqual({ field: "CUSTOMER_PRIMARY_EMAIL" }); current = { ...current, request: { ...current.request, state: "REVIEW_PENDING", version: 2 }, allowedActions: [], canViewRevealedValue: true }; return HttpResponse.json({ revealAttemptId: id, requestId: id, caseId: id, subjectId: id, field: "CUSTOMER_PRIMARY_EMAIL", value: "emergency@example.invalid", revealedAt: "2026-09-11T09:04:00Z" }); })); await userEvent.click(await canvas.findByRole("button", { name: "긴급 정보 한 번 열람" })); await expect(await canvas.findByText("emergency@example.invalid")).toBeVisible(); window.dispatchEvent(new Event("blur")); await waitFor(() => expect(canvas.queryByText("emergency@example.invalid")).not.toBeInTheDocument()); await expect(canvas.queryByRole("button", { name: "긴급 정보 한 번 열람" })).not.toBeInTheDocument(); } };
export const LateResponseAfterLeave: Story = { beforeEach: active, play: async ({ canvas, msw }) => { msw.use(http.post("/api/v1/support/break-glass-requests/:id/reveals", async () => { window.dispatchEvent(new Event("blur")); await delay(100); current = { ...current, request: { ...current.request, state: "REVIEW_PENDING" }, allowedActions: [], canViewRevealedValue: true }; return HttpResponse.json({ revealAttemptId: id, requestId: id, caseId: id, subjectId: id, field: "CUSTOMER_PRIMARY_EMAIL", value: "late@example.invalid", revealedAt: "2026-09-11T09:04:00Z" }); })); await userEvent.click(await canvas.findByRole("button", { name: "긴급 정보 한 번 열람" })); await expect(await canvas.findByText("독립 사후 검토 대기")).toBeVisible(); await expect(canvas.queryByText("late@example.invalid")).not.toBeInTheDocument(); } };
export const UnknownReveal: Story = { beforeEach: active, play: async ({ canvas, msw }) => { let calls = 0; msw.use(http.post("/api/v1/support/break-glass-requests/:id/reveals", () => { calls++; return HttpResponse.error(); })); await userEvent.click(await canvas.findByRole("button", { name: "긴급 정보 한 번 열람" })); await expect(await canvas.findByText("긴급 열람 응답을 확인하지 못했습니다")).toBeVisible(); await userEvent.click(canvas.getByRole("button", { name: "긴급 요청 상태 새로고침" })); await waitFor(() => expect(canvas.queryByRole("button", { name: "긴급 정보 한 번 열람" })).not.toBeInTheDocument()); expect(calls).toBe(1); } };
export const IndependentReview: Story = { beforeEach() { current = { request: { ...request, state: "REVIEW_PENDING", version: 2, expiresAt: "2026-09-11T09:03:00Z" }, allowedActions: ["REVIEW"], canViewRevealedValue: false, postReview: null }; }, play: async ({ canvas, msw }) => { msw.use(http.post("/api/v1/support/break-glass-requests/:id/reviews", async ({ request: httpRequest }) => { expect(await httpRequest.json()).toEqual({ decision: "ESCALATED", expectedVersion: 2, reasonCode: "ADDITIONAL_REVIEW_REQUIRED" }); current = { ...current, request: { ...current.request, state: "REVIEWED", version: 3 }, allowedActions: [], postReview: { decision: "ESCALATED", reasonCode: "ADDITIONAL_REVIEW_REQUIRED", decidedAt: "2026-09-11T09:04:00Z" } }; return HttpResponse.json(current.request); })); await userEvent.selectOptions(await canvas.findByLabelText("긴급 열람 사후 검토"), "ESCALATED"); await userEvent.click(canvas.getByRole("button", { name: "사후 검토 결과 기록" })); await expect(await canvas.findByText("사후 검토 결과를 기록했습니다")).toBeVisible(); await expect(canvas.queryByText("emergency@example.invalid")).not.toBeInTheDocument(); } };
export const Expired: Story = { beforeEach() { active(); current.request.expiresAt = "2026-09-11T09:03:00Z"; current.allowedActions = []; }, play: async ({ canvas }) => { await expect(await canvas.findByText("열람 기한 만료")).toBeVisible(); await expect(canvas.queryByRole("button", { name: "긴급 정보 한 번 열람" })).not.toBeInTheDocument(); } };
export const PermissionLost: Story = { play: async ({ canvas, msw }) => { await canvas.findByRole("button", { name: "긴급 요청 상태 새로고침" }); msw.use(http.get("/api/v1/support/break-glass-requests/:id/workflow", () => HttpResponse.json({ code: "ACCESS_DENIED", correlationId: "BREAK-GLASS-REVOKED" }, { status: 403 }))); await userEvent.click(canvas.getByRole("button", { name: "긴급 요청 상태 새로고침" })); await expect(await canvas.findByText("문의 코드 BREAK-GLASS-REVOKED")).toBeVisible(); await expect(canvas.queryByRole("button", { name: "긴급 열람 승인" })).not.toBeInTheDocument(); } };
export const UnknownApproval: Story = { play: async ({ canvas, msw }) => { let key = "", calls = 0; msw.use(http.post("/api/v1/support/break-glass-requests/:id/approvals", async ({ request: httpRequest }) => { expect(await httpRequest.json()).toEqual({ decision: "DENY", expectedVersion: 0 }); calls++; if (calls === 1) { key = httpRequest.headers.get("Idempotency-Key")!; return HttpResponse.error(); } expect(httpRequest.headers.get("Idempotency-Key")).toBe(key); current = { ...current, request: { ...request, state: "DENIED", version: 1 }, allowedActions: [] }; return HttpResponse.json(current.request); })); await userEvent.click(await canvas.findByRole("button", { name: "긴급 열람 반려" })); await userEvent.click(await canvas.findByRole("button", { name: "같은 긴급 결정 확인" })); await expect(await canvas.findByText("열람 반려")).toBeVisible(); expect(calls).toBe(2); } };
