# Minor Decisions

| ID | Date | Task | Decision | Rationale | Affected files | Revisit |
|---|---|---|---|---|---|---|
| MD-2026-001 | 2026-07-28 | 주문 생성 예약 lease Milestone 1 | 실제 영속 event producer가 없는 동안 `spring-modulith-starter-jpa`를 활성화하지 않는다 | Hibernate `validate`가 사용하지 않는 Event Publication Entity schema를 요구하지 않게 하고, ADR-010의 구체 publication 선택은 첫 event-driven Feature에서 검증한다 | `build.gradle.kts`, 애플리케이션 context | 영속 cross-module event producer를 구현할 때 |
| MD-2026-002 | 2026-08-01 | PR CI 고속화와 실패 전파 복구 | required `build` job은 모든 PR에서 유지하되 문서·OpenAPI-only 변경은 정적 검증만 실행하고, 그 밖의 변경과 분류 불명은 전체 Gradle build를 실행한다. 모든 검증 로그 pipeline은 원 명령의 실패를 보존한다 | required workflow 자체를 건너뛰면 merge가 Pending에 막힐 수 있다. 문서 계약은 빠르게 검증하되 unknown 변경과 검사 도구 변경은 fail-closed로 전체 build에 보내고, 실패를 성공으로 위장하지 않는다 | `.github/workflows/ci.yml`, `scripts/ci/`, `scripts/verify-docs.sh` | fast path 오분류, OpenAPI 검증 누락 또는 전체 build 회귀가 관측될 때 |
| MD-2026-003 | 2026-08-01 | Signed cursor test context | 공개 test-vector key는 `src/test/resources`의 test application context에만 둔다 | main·local runtime 설정에 secret/fallback을 추가하지 않으면서 통합 테스트가 실제 required key-ring binding을 사용하게 한다 | `src/test/resources/application.yaml`, Cursor tests | test context가 explicit properties로 전환되거나 test vector가 더 이상 필요 없을 때 |
| MD-2026-004 | 2026-08-01 | Signed cursor canonical JSON writer | codec 내부 기본 ObjectMapper와 insertion-ordered map으로 v1 payload를 직렬화한다 | 전역 Jackson customization이 property order나 whitespace 없는 고정 wire bytes를 바꾸지 않게 한다 | `HmacSignedCursorCodec.kt` | versioned wire contract가 새 codec writer 또는 payload type을 필요로 할 때 |
| MD-2026-005 | 2026-08-01 | CodeQL Kotlin autobuild heap | CodeQL default setup의 Kotlin compiler daemon heap 상한을 project `gradle.properties`에서 `-Xmx2048m`으로 고정한다 | PR #29의 default autobuild가 `testClasses` 중 Kotlin compiler heap OOM으로 실패했다. analyzer·언어 범위·Gradle JVM을 바꾸지 않고 compiler에만 충분한 상한을 부여한다 | `gradle.properties` | CodeQL standard runner에서 재현된 OOM, 과도한 메모리 압박 또는 build policy 변경이 관측될 때 |
| MD-2026-006 | 2026-08-06 | Source OpenAPI 역할 명확화 | `beanflow-v1.yaml`은 Accepted target, `beanflow-v1-runtime.yaml`은 현재 source의 Controller mapping과 계약 테스트가 존재하는 계약으로 정의하고 `deployed` 명칭을 제거한다 | 저장소에는 non-local deployment 증거가 없고 기존 파일 설명도 source 구현을 가리켰다. Runtime operation은 `RequestMappingHandlerMapping` parity test로 검증해 별도 수동 allowlist를 두지 않는다 | OpenAPI, README, API conventions, 문서 검증, Runtime parity test | 실제 배포 inventory와 promotion 자동화가 도입되어 source runtime과 별도 deployment 계약이 필요할 때 |
| MD-2026-007 | 2026-08-06 | Nearby Discovery Testcontainers image | 모든 테스트가 `postgis/postgis:17-3.5`를 쓰도록 공통 상수 `BEANFLOW_POSTGRES_IMAGE`로 고정한다 | V33이 모든 환경에서 PostGIS extension을 요구하므로 plain PostgreSQL image로는 spatial 테스트가 통과할 수 없어야 한다. 이 image에는 `linux/amd64` manifest만 있고 CI runner는 amd64, Apple Silicon workstation은 Docker 에뮬레이션으로 실행한다 | `BeanflowPostgresImage.kt`, `TestcontainersConfiguration.kt`, 12개 migration test | 공식 image가 arm64 manifest를 제공하거나 PostgreSQL/PostGIS major version을 올릴 때 |
| MD-2026-008 | 2026-08-06 | Nearby query parameter binding | latitude/longitude/radiusMeters/limit/cursor를 raw `String`으로 받고 Discovery가 직접 검증해 값이 없는 `INVALID_REQUEST`만 반환한다. **개정 (2026-08-08):** 입력 문법은 OpenAPI `type: number, format: double`의 유한 부분집합이다. 선행 부호, 소수부, 최대 4자리 exponent를 허용하고 `NaN`/`Infinity`/16진수/Java float suffix는 거부한다. filter hash의 canonical form은 여전히 plain decimal이며 `37.5`, `+37.5`, `37.50`, `3.75e1`은 같은 hash로 정규화된다. 범위 검증은 문법 검증과 분리한다. **개정 (2026-08-10):** 공용 handler가 rejected value와 exception message 반사를 중단했다. raw String 계약은 Discovery의 명시적 문법·범위 검증과 signed cursor canonicalization을 유지하기 위해 계속 사용한다 | 초기 결정 당시 typed binding은 공용 handler가 원본 좌표를 error body에 반사해 BR-28을 위반했다. 2026-08-10 이후 이 노출 근거는 해소됐지만, Discovery가 허용 문법과 canonical hash를 한 경계에서 결정하는 구조는 공개 계약과 일치한다 | `NearbyStoreQueryController.kt`, `NearbyStoreQueryService.kt`, `ApiExceptionHandler.kt` | 좌표 입력 문법 또는 cursor canonicalization 책임을 바꿀 때 |
| MD-2026-009 | 2026-08-06 | StoreDiscoveryProfile 영속 매핑 | profile을 JPA Entity로 매핑하지 않고 Merchant가 JDBC native DTO projection만 소유한다 | `geography` 매핑에 새 persistence dependency가 필요하고 `ddl-auto: validate`와 충돌한다. Store 쓰기 Entity에 검색용 객체 그래프를 붙이지 않는다는 ADR-020 결정과도 맞는다 | `StoreDiscoveryProfileQueryRepository.kt`, `MerchantPersistence.kt` | profile 쓰기 use case가 생겨 Store와 같은 transaction의 Entity 매핑이 필요할 때 |
| MD-2026-010 | 2026-08-07 | Discovery pickup slot 조회 범위 | **Superseded (2026-08-08) — [ADR-076](../adr/ADR-076-store-catalog-read-contract.md)과 BR-05 Slot Reservation Window Amendment로 대체됨.** 원 결정: `GET /stores/{storeId}/pickup-slots`는 주입된 Clock 기준 `ends_at > now`인 슬롯만 `(starts_at, id)` 순서로 반환하고 `PickupReservationService.reserve`의 슬롯 시간 미검증 동작은 바꾸지 않는다 | 원 근거: 계약이 "available pickup slots"이고 종료된 슬롯은 픽업할 수 없으나, 쓰기 경로 변경은 별도 제품 결정이 필요하므로 read와 write 창이 다르다는 사실을 문서에 남긴다. 이후 조회 창은 공개 계약을 바꾸고 쓰기 경로의 시간 검증과 함께 결정해야 하므로 Minor Decision 분류가 맞지 않다고 판단해 승격했다 | `PickupSlotQueryOperations.kt`, `PickupSlotQueryRepository.kt`, nearby runbook | 대체 완료. 이후 변경은 ADR-076에서 다룬다 |
| MD-2026-011 | 2026-08-07 | Discovery 메뉴 목록 availability 투영 | `GET /stores/{storeId}/menus`는 store의 메뉴와 옵션을 모두 반환하고 `available`에 현재 owner state를 그대로 투영한다 | `Menu.available`과 `MenuOption.available`이 required 필드이므로 판매 가능한 항목만 거르면 계약이 무의미해진다. `merchant_menu`에는 별도 visibility 컬럼이 없어 없는 모델을 추정하지 않는다 | `StoreMenuQueryOperations.kt`, `StoreMenuQueryRepository.kt` | 별도 visibility/노출 정책과 컬럼이 도입될 때 |

| MD-2026-012 | 2026-08-12 | 점주 매출 분석 지표 소유권 | 점주 콘솔 `3a 매출 분석`은 새 지표를 정의하지 않고 BR-31의 두 지표(환불 발생일 기준 환불액, 원 주문 완료일 기준 보정 수익)만 소비한다. 객단가·환불률·인기 메뉴는 그 두 지표와 주문 스냅샷에서 파생하고, 지표 정의는 Analytics Context가 단독 소유한다. 따라서 점주 매출 분석은 [Analytics ExecPlan](../exec-plans/active/analytics-refund-and-late-event-projection.md) 완료 이후의 P1이다 | 같은 이름의 지표가 두 Context에서 다른 숫자를 내면 점주가 보는 예상 정산액과 실제 정산 명세가 어긋난다. BR-31과 ADR-023이 이미 귀속일 규칙을 확정했으므로 Merchant가 다시 정의할 이유가 없다 | `docs/product/design-to-capability-map.md`, 향후 Merchant analytics query | 점주 화면이 Analytics의 두 지표로 표현할 수 없는 요구를 갖게 될 때 |
| MD-2026-013 | 2026-08-12 | Session·CSRF target wire naming | 고객·점주 인증 Cookie target 이름은 각각 `BEANFLOW_CUSTOMER_SESSION`, `BEANFLOW_MERCHANT_SESSION`으로 고정한다. CSRF GET은 body 없이 204로 actor별 JS-readable XSRF Cookie만 발급하고 unsafe 요청은 공통 `X-BEANFLOW-CSRF` header를 사용한다 | ADR-094가 actor별 Cookie 분리와 XSRF 이름/header를 정했지만 인증 Cookie의 exact 이름과 CSRF 응답 body는 정하지 않았다. target OpenAPI가 두 Chain을 오해 없이 표현하려면 exact wire name이 필요하며, token body를 중복 노출할 이유가 없다 | `openapi/beanflow-v1.yaml`, productization-20 | BFF, 다른 origin 배치 또는 framework 제약으로 Cookie wire 계약을 바꿔야 할 때 |
| MD-2026-014 | 2026-08-15 | Stack A CSRF consumer 보정 소유권 | **Resolved (2026-08-16) — PR #74의 shared customer/merchant CSRF helper와 세 call site header 보정으로 `npm run typecheck`이 clean해졌다. temporary baseline command와 CI step은 제거한다. Plan 80·90은 actor별 client 분리와 화면 교체라는 더 넓은 milestone을 계속 소유한다.** 원 결정: runtime OpenAPI가 요구하는 기존 unsafe 요청 세 곳의 `X-BEANFLOW-CSRF` 보정은 완료된 Plan 60에 one-off header로 선반영하지 않는다. Customer `payment-attempt`·`payment-confirmation`은 Plan 80의 Customer Session/CSRF client milestone에서, legacy Merchant `POST /payments/{paymentId}/refunds`는 Plan 90의 Merchant financial Session/CSRF client와 UUID form 교체 milestone에서 각각 함께 전환한다 | 실제 코드가 세 header를 모두 전송해 known-error baseline의 전제가 사라졌다. 오류를 의도적으로 성공으로 허용하거나 clean typecheck를 실패로 바꾸지 않고, CI는 direct typecheck로 새 회귀를 실패시킨다 | `CustomerPages.tsx`, `ConsolePages.tsx`, `frontend/src/api/client.ts`, `.github/workflows/ci.yml`, productization-60/80/90 ExecPlans | actor별 client 분리 또는 기존 UUID form 제거의 scope를 변경할 때 |
| MD-2026-015 | 2026-08-15 | 검색 term 가중치와 색인 정규화 | 검색 term 가중치를 `STORE_NAME 1.00`, `BRAND_NAME 0.90`, `REGION_* 0.80`, `MENU_NAME 0.70`으로 고정하고 `discovery_store_search_term.weight` 컬럼에 비정규화해 저장한다. 정규화는 NFKC → 소문자 → 연속 공백 축약 → trim 순의 단일 함수이며 색인 기록과 질의 양쪽이 같은 함수를 사용한다 | 가중치를 `CASE` 분기로 계산하면 정렬·집계마다 반복되고 인덱스 전용 스캔을 막는다. 값 자체는 BR-47이 정한 순서 관계만 지키면 되는 튜닝 상수라 ADR이 아닌 Minor Decision이 맞다. 정규화 함수가 색인과 질의에서 갈라지면 검색이 조용히 0건을 반환하므로 한 곳에만 둔다 | `SearchTextNormalizer.kt`, `discovery_store_search_term` migration, 검색 Query Repository | 2026-08-15 `REGION_RI` 추가로 재검토했고 `REGION_*` 묶음 가중치 `0.80`을 그대로 적용해 서열은 바꾸지 않았다. 다시 상대 순서를 바꾸거나 term 종류를 추가할 때, 또는 실제 검색 품질 측정으로 가중치를 재조정할 때 |
| MD-2026-016 | 2026-08-15 | 법정동 시드 생성 방식 | 행정안전부 법정동 코드 원본 파일을 저장소에 넣지 않고 `scripts/generate-region-seed.py`가 다운로드 URL과 checksum을 검증해 결정적으로 변환한 시드 SQL만 커밋한다. 계층 분해는 법정동명 공백이 아니라 코드 자릿수(`2+3+3+2`)로 하고 시도·시군구·읍면동·리 네 열을 채운다. 시드는 정렬된 `INSERT ... ON CONFLICT DO NOTHING`이라 재실행 가능하다 | 원본은 인코딩·구분자가 배포 시점마다 다르고 폐지 항목을 포함한다. 변환 결과만 커밋하면 migration이 결정적이고, 생성 스크립트가 남아 있어 행정구역 개편 시 재생성 경로가 사라지지 않는다. 시드 SQL은 약 2MB로 Flyway가 단일 파일로 처리한다 | `scripts/generate-region-seed.py`, 법정동 시드 migration | 시드 파일 크기가 build/CI에 부담이 되거나 행정구역 개편 반영 주기가 운영 부담이 될 때 |
| MD-2026-017 | 2026-08-15 | 설정 주소지의 보관 위치 | "설정 주소지 인근 검색"의 주소지는 브라우저 client storage에만 두고 매 요청 좌표를 전송한다. 서버는 좌표를 요청 범위에서만 사용하고 계정에 주소·좌표 컬럼을 추가하지 않으며 공개 API 계약도 바꾸지 않는다. 검토한 대안은 (a) 법정동 코드만 계정에 저장 — 반경 대신 행정구역 일치가 되어 동 경계 밖 매장이 빠진다, (b) 법정동 대표 좌표로 반경 검색 — 법정동 코드 파일에 좌표가 없어 별도 중심점 데이터셋이 필요하다, (c) 정밀 좌표 저장 — ADR-020 개정과 동의·보존·삭제 정책이 필요하다 | 요구를 서버 보존 없이 그대로 충족할 수 있으므로 개인정보 보존 면적을 늘릴 이유가 없다. 서버 계약 변경이 0이라 되돌리기도 쉽다. 대가로 주소지가 기기·브라우저에 묶여 기기 간 동기화가 되지 않고 client storage를 지우면 사라진다. 이 한계를 기능 결함으로 숨기지 않는다 | frontend client storage, `productization-70` 프론트엔드 계약 절, [ADR-020](../adr/ADR-020-nearby-location-privacy.md) | 기기 간 동기화 또는 계정 귀속 주소가 실제 요구가 될 때. 그때 BR-28 Revisit Condition에 따라 동의·보존·삭제 정책과 함께 ADR-020을 다시 연다 |
| MD-2026-018 | 2026-08-15 | 검색 색인 초기 적재의 실행 위치 | `discovery_store_search_term`의 초기 적재를 migration SQL 백필이 아니라 애플리케이션 재색인(`StoreSearchIndexRebuildService`)이 수행한다. V59는 스키마만 만들고 행을 넣지 않는다 | MD-2026-015가 색인과 질의의 정규화를 한 함수로 못박았는데 SQL 백필은 그 함수를 부를 수 없어 `normalize(NFKC) + lower()`로 다시 구현해야 한다. Testcontainers PostgreSQL 17에서 두 구현을 직접 비교한 결과 `İSTANBUL`은 SQL `istanbul` / Kotlin `i̇stanbul`(U+0069 U+0307), `ΟΔΟΣ`는 SQL `οδοσ` / Kotlin `οδος`로 갈렸다. Java `String.lowercase()`는 Unicode SpecialCasing과 어말 시그마 조건을 적용하지만 PostgreSQL `lower()`는 적용하지 않는다. 문자를 개별 치환해 맞추는 방식은 Unicode 규칙이 늘 때마다 조용히 어긋나므로 두 번째 구현 자체를 두지 않는다. 대가로 V59 적용 직후 색인이 비어 있고 커버리지 gauge가 `0`이며, 운영자가 재색인을 실행해야 `1.0`이 된다. 이 창을 숨기지 않고 gauge와 runbook으로 드러낸다 | `V59__create_store_search_term_index.sql`, `StoreSearchIndexRebuildService.kt`, `docs/operations/store-keyword-search-runbook.md` | 매장·메뉴 쓰기 API가 생겨 쓰기 커맨드가 동기 색인 갱신을 흡수할 때. 그때 초기 적재 이후의 재색인은 복구 수단으로만 남는다 |
| MD-2026-019 | 2026-08-15 | 브랜드 명령의 재실행 원장 | 브랜드 명령의 멱등성을 `merchant_brand_command` 원장으로 구현하고 이를 위해 계획에 없던 네 번째 migration `V60`을 추가한다. 원장은 확정 응답을 `response_json`에 담아 재요청이 새 명령을 실행하지 않고 같은 결과를 돌려주게 하며, 멱등 키 범위는 `(actor_id, idempotency_key)`다. V60은 `STORE_BRAND_MANAGE` 권한과 브랜드 감사 action 넷도 함께 등록한다 | 저장소의 모든 `Idempotency-Key` 명령은 전용 원장 테이블을 쓴다(V16, V23, V26, V31, V54). 브랜드만 다른 방식을 쓸 이유가 없고, AuditRecord는 `source_reference`가 전역 유일하지 않아 원장을 겸할 수 없다. `command_type`을 유일 키에 넣지 않은 이유는 같은 키를 생성과 지정에 나눠 쓰는 것도 재사용이기 때문이다. `command_type`은 payload hash에 들어가므로 그 요청은 409가 된다. 계획의 단계 3(지역 커버리지 gate)은 `V61`로 밀린다 | `V60__create_merchant_brand_command.sql`, `BrandPersistence.kt`, `StoreBrandService.kt` | 공용 멱등 원장 추상화가 생기거나, 원장 보존 기간 90일이 운영 부담이 될 때 |
| MD-2026-020 | 2026-08-15 | 브랜드 보관과 fan-out 상한의 적용 지점 | ADR-112가 정하지 않은 두 규칙을 구현에서 확정한다. (1) 소속 매장이 남은 브랜드의 보관은 `409 BRAND_STATE_CONFLICT`로 거절한다. (2) ADR-112 6절의 1000개 상한을 이름 변경뿐 아니라 **매장 배정**에도 적용해 상한을 넘기는 배정을 거절한다 | (1) 보관은 활성 unique index에서 빠지는 것이라 정규화 이름을 즉시 해방한다. 매장을 남긴 채 보관하면 새 브랜드가 같은 이름을 차지해 색인에 서로 다른 브랜드의 같은 `BRAND_NAME` term이 공존한다. (2) 상한을 배정에서 막지 않으면 1000개를 넘긴 브랜드는 이름을 영영 바꿀 수 없게 된다. 나중에 이름 변경이 갑자기 막히는 것보다 배정 시점에 거절하는 편이 낫다. 매장·메뉴 쓰기 API가 없어 직접 DML로 상한을 넘긴 상태는 여전히 도달 가능하므로 이름 변경 쪽 검사도 남긴다 | `StoreBrandService.kt`, `docs/api/error-catalog.md`, [ADR-112](../adr/ADR-112-store-brand-and-administrative-region.md) | 브랜드 소속 매장이 1000개 상한에 도달해 ADR-112 Revisit Condition이 열릴 때, 또는 보관된 브랜드에 매장을 남기는 운영 요구가 생길 때 || MD-2026-021 | 2026-08-15 | 지역 명령 원장과 커버리지 gate의 migration 분리 | 매장 지역 명령을 `merchant_store_region_command` 전용 원장으로 구현하고 원장·감사 action은 `V61`, 커버리지 gate는 `V62`로 **나눈다**. 계획의 단계 3 gate는 `V61`이었으나 `V62`로 밀린다 | MD-2026-019가 세운 명령군별 원장 규약을 따른다. 브랜드 원장에 얹지 않는 이유는 행위자가 다르기 때문이다. 브랜드는 운영자, 지역은 매장주이므로 `(actor_id, idempotency_key)` 유일성이 서로 다른 주체 집합 위에서 성립한다. 둘을 한 migration에 담을 수 없는 이유는 배포 순서다. 기존 매장이 있는 환경에서는 원장이 생겨 명령을 실행할 수 있게 된 뒤에야 지역을 채울 수 있는데, gate가 같은 migration에 있으면 값을 넣을 순간이 없다. V33 -> V34 선례와 같은 두 단계 배포다 | `V61__create_merchant_store_region_command.sql`, `V62__verify_store_region_coverage.sql`, `RegionPersistence.kt`, `StoreRegionService.kt` | 공용 멱등 원장 추상화가 생기거나, 지역 해제 명령이 필요해질 때 |
| MD-2026-022 | 2026-08-15 | 지역 감사 요약의 법정동 코드 표기 | `STORE_REGION_ASSIGNED` 감사 요약은 법정동 코드를 코드 자체의 계층 구분(시도 2 · 시군구 3 · 읍면동 3 · 리 2)으로 끊어 `11-680-101-00` 형태로 담는다. 지역 표시 이름은 담지 않는다 | `AuditRecordService`의 원시 PII 판정기가 10자리 법정동 코드를 한국 휴대전화 번호 패턴으로 인식해 append를 거절한다. 개인정보 보호 장치를 통과시키려고 완화하지 않고 payload 쪽을 바꾼다. 구분자는 만들어 낸 형식이 아니라 법정동 코드의 실제 구조이고 하이픈만 빼면 저장된 값이 그대로 나오므로 손실이 없다. 표시 이름을 뺀 이유는 별개다. 민감 키 규칙이 `fullName`을 포함하는 키를 거절하며, 이름은 코드로부터 폐쇄 어휘를 통해 언제든 되찾을 수 있다 | `StoreRegionCommandService.kt`, `StoreRegionEndpointIntegrationTest.kt` | 원시 PII 판정기가 코드 형식 값을 구분하게 되거나, 감사 요약 스키마가 형식을 갖게 될 때 |
| MD-2026-023 | 2026-08-15 | 검색 후보 질의의 소유 모듈 | 매장 통합 검색의 후보 질의를 계획서의 이름(`MerchantStoreSearchQuery`)과 달리 `merchant`가 아니라 **`discovery`**에 두고, 그 SQL이 `merchant_store_discovery_profile`과 `merchant_store`를 직접 조인한다 | 후보 질의는 색인 테이블과 매장 프로필을 **한 문장에서** 함께 읽어야 한다. 토큰 매칭 결과가 무한정 커질 수 있어 중간 집합을 port로 실어 나를 수 없기 때문이다. 그러면 두 모듈 중 한쪽의 SQL이 상대 테이블을 읽게 되는데, ADR-112 5절이 「`discovery`의 검색은 매장 상태를 읽어야 한다」와 「`merchant`는 `discovery`를 모른다」를 함께 못박았다. 저장소의 기존 cross-module SQL(`ordering` -> `merchant`·`fulfillment`, `operations` -> `ordering`·`payment`)도 모두 기존 모듈 의존 방향을 따르며 역방향 사례는 하나도 없다. `merchant`가 색인 테이블을 읽는 배치는 그 유일한 역방향이 된다 | `StoreSearchCandidateRepository.kt`, [ADR-112](../adr/ADR-112-store-brand-and-administrative-region.md) 5절, [ADR-103](../adr/ADR-103-store-search-strategy.md) A1 | 색인 테이블의 소유 모듈이 바뀌거나, cross-module 조인을 대체할 read model이 생길 때 |
| MD-2026-024 | 2026-08-15 | trigram 임계값을 transaction 지역 설정으로 고정 | 검색 질의는 `set_config('pg_trgm.similarity_threshold', '0.3', true)`로 임계값을 transaction에 고정하고, **그와 별개로** 질의 안에서 `similarity(...) >= 0.3`을 다시 비교한다 | 계획은 「세션 설정에 의존하지 않도록 쿼리에서 임계값을 명시 비교」만 요구했는데 그것만으로는 한쪽 방향밖에 막지 못한다. `%` 연산자가 GIN 인덱스를 타는 유일한 경로인데 그 판정은 세션 GUC를 쓴다. 세션 임계값이 `0.3`보다 낮으면 `%`가 상위 집합을 내고 명시 비교가 걸러 주지만, 더 높으면 `%`가 이미 부분 집합을 내버려 명시 비교가 손쓸 수 없다. 두 방향을 함께 닫으려면 설정 고정이 필요하다. `SET LOCAL`이므로 commit과 함께 되돌아가고 다른 세션에 영향을 주지 않는다 | `StoreSearchCandidateRepository.kt`, `StoreSearchCandidateRepositoryIntegrationTest.kt` | `%` 없이도 GIN 인덱스를 타는 연산자를 쓰게 되거나, 임계값이 요청 파라미터가 될 때 |
| MD-2026-025 | 2026-08-15 | 픽업 가용성 batch port의 이름과 반환 모양 | Fulfillment의 batch 판정 port를 계획서의 이름(`FulfillmentPickupAvailabilityQuery.existsByStoreIds`)과 달리 `PickupAvailabilityQueryOperations.findStoresWithAvailableSlots(storeIds, now): Set<UUID>`로 둔다 | 모듈의 기존 공개 port가 모두 `*Operations`이고(`PickupSlotQueryOperations`, `PickupReservationOperations`, `FulfillmentSupportTimelineOperations`) 이름 하나만 규약에서 벗어날 이유가 없다. 반환형을 `Set<UUID>`로 둔 이유는 별개다. `existsBy...`라는 이름은 boolean 하나를 시사하는데 실제로 필요한 것은 후보별 판정이고, `Map<UUID, Boolean>`으로 두면 "없는 키"와 "false"라는 같은 뜻의 두 표현이 생겨 호출부가 둘 다 다뤄야 한다. 판정 자체는 계획·ADR-103과 동일하다 | `PickupAvailabilityQueryOperations.kt`, `PickupAvailabilityQueryService.kt`, [ADR-103](../adr/ADR-103-store-search-strategy.md) | 가용 슬롯 수나 가장 이른 슬롯 시각처럼 존재 여부보다 많은 정보가 필요해질 때 |
| MD-2026-026 | 2026-08-15 | nearby가 소유자 상태 hard filter를 유지 | `GET /stores/nearby`는 `pickupAvailable` 의미를 통일하면서도 SQL의 `accepting_orders AND pickup_enabled` 필터를 그대로 둔다. 즉 nearby는 `openOnly=true`가 항상 켜진 endpoint이고, 새 `pickupAvailable=true`는 그 위에 슬롯 존재를 더하는 필터다. 「미지정이면 닫힌 매장도 포함」은 `/stores/search`에만 적용한다 | ADR-103이 nearby에 대해 바꾸라고 한 것은 `pickupAvailable`의 **판정**이지 결과 집합의 구성이 아니다. hard filter를 걷어내면 nearby가 갑자기 닫힌 매장을 반환하게 되는데, 그것은 요청받지 않은 공개 계약 변경이고 BR-47의 「기본은 닫힌 매장 포함」은 검색 endpoint의 문장이다. 대가로 nearby에는 `openOnly` 파라미터가 없고 그 상태를 끌 수 없다. 이 비대칭을 숨기지 않고 OpenAPI 설명에 명시한다 | `StoreDiscoveryProfileQueryRepository.kt`, `openapi/beanflow-v1.yaml`, [ADR-103](../adr/ADR-103-store-search-strategy.md) | nearby에서도 닫힌 매장을 상태 플래그와 함께 보여 달라는 요구가 생길 때. 그때는 `openOnly` 파라미터 추가와 ADR-070 filter hash 개정을 함께 한다 |
| MD-2026-027 | 2026-08-15 | 공개 `pickupAvailable`을 소유자 상태와 슬롯 존재의 AND로 구성 | 후보 질의의 매장 상태 필드를 `pickupAvailable`이 아니라 `pickupCapable`(= `acceptingOrders && pickupEnabled`)로 이름 짓고, 공개 `pickupAvailable`은 서비스 계층에서 `pickupCapable && 슬롯 존재`로 만든다. Merchant의 `NearbyStoreProfileProjection`에서는 해당 필드를 **삭제**한다 | ADR-103의 정의가 두 조건의 AND인데(「`acceptingOrders && pickupEnabled`이고, ... 슬롯이 하나 이상 있다는 뜻」) SQL은 슬롯을 볼 수 없다. 필드 이름을 공개 플래그와 같게 두면 SQL이 아는 절반이 공개 플래그처럼 보이고, 실제로 Milestone 6 이전의 약한 의미는 정확히 그 경로로 응답에 도달했다. Merchant 쪽은 이름을 고치는 대신 필드를 없앴다. 소유자 상태만 아는 모듈이 그 필드를 갖고 있으면 재도출이 언제든 다시 가능해지기 때문이다 | `StoreSearchCandidateRepository.kt`, `StoreSearchQueryService.kt`, `StoreDiscoveryQueryOperations.kt` | 슬롯 존재를 후보 질의와 같은 문장에서 읽게 되거나(모듈 경계 변경), `open`/`pickupAvailable` 외의 상태 플래그가 추가될 때 |
| MD-2026-028 | 2026-08-16 | customer discovery의 작은 cross-context contract 경계 | `CustomerRecentStoreQuery`와 운영자 `StoreSearchIndexRebuildOperations`의 interface·DTO를 `shared/api`에 둔다. 구현과 데이터 소유는 각각 `ordering/internal`, `discovery/internal`에 남기며, compact projection/명령 결과만 넘긴다 | Spring Modulith는 public `ordering/api`·`discovery/api`를 노출해도 모든 module 방향을 허용하지 않는다. 실제 `ModularityTests`가 Discovery → Ordering 및 Operations → Discovery를 거절했고 후자는 `discovery → fulfillment → operations → discovery` 순환을 만들었다. `shared/api`에는 이미 Merchant가 호출하고 Discovery가 구현하는 색인 write contract가 있어 같은 boundary pattern을 재사용할 수 있다. shared에 table access나 일반 query broker를 추가하지 않으므로 owner와 Aggregate 경계는 변하지 않는다 | `CustomerRecentStoreQuery.kt`, `StoreSearchIndexRebuildOperations.kt`, M8/M10 services | 필요한 데이터가 compact contract로 표현할 수 없거나, shared API가 implementation discovery/service locator로 커지기 시작할 때 |
| MD-2026-029 | 2026-08-16 | 고객 주문 상세의 재주문 픽업 시간 조회 | `CustomerOrderDetail`에 서버가 주는 opaque `storeId`를 추가한다. 재주문 화면은 이 값으로 해당 매장의 현재 픽업 슬롯만 조회하고, 어떤 화면도 storeId를 입력 field로 받지 않는다 | 재주문은 `pickupSlotId`가 필수인데 주문 상세에는 매장 식별자가 없어 고객이 픽업 시간을 고를 방법이 없었다. 검토한 대안은 (a) 매장 이름으로 최근 매장 목록과 대조 — 동명 매장에서 틀리고 이름 변경에 깨진다, (b) 재주문에서 슬롯을 서버가 임의 선택 — 고객이 고르지 않은 픽업 시간을 확정하게 된다. 응답으로 받은 opaque ID를 그대로 쓰는 것은 이미 허용된 계약이라 입력 field 금지 규칙과 충돌하지 않는다 | `CustomerOrderQueryContracts.kt`, `CustomerOrderQueryRepository.kt`, `CustomerOrderQueryService.kt`, `openapi/beanflow-v1.yaml`, `features/ordering/ReorderPanel.tsx` | 주문 상세가 매장 식별자 없이 픽업 슬롯을 고를 수 있는 계약을 갖게 될 때 |
| MD-2026-030 | 2026-08-17 | 부분 환불 실패 code의 정밀화와 pure allocation 추출 | 부분 환불 준비 transaction이 던지던 두 `ORDER_STATE_CONFLICT`를 상황별 code로 나눈다. 미확정 Refund는 `REFUND_OUTCOME_UNRESOLVED`(409), 잔여 unit 초과 수량은 새 `REFUND_QUANTITY_UNAVAILABLE`(422)이다. unit 배분과 잔여 수량 계산은 `RefundAllocationCalculator`로 추출해 preview projection과 실행 transaction이 같은 함수를 부른다 | 두 code 모두 error catalog가 이미 요구하는 구분이고(preview 재조회 가능 vs reconciliation 대기), 하나의 code로는 UI가 다음 행동을 고를 수 없다. 수량 초과는 요청 자체가 처리 불가라 semantics상 422이며 legacy endpoint의 OpenAPI에도 422가 이미 선언돼 있다. 계산을 복제하지 않고 추출한 이유는 preview 금액과 실행 금액이 갈라지면 stale 판정 자체가 무의미해지기 때문이다 | `RefundAllocationCalculator.kt`, `PartialRefundService.kt`, `PartialRefundPaymentService.kt`, `ApiExceptionHandler.kt`, `docs/api/error-catalog.md` | 부분 환불 계약에 새 배분 단위(묶음 상품·세금·외화)가 생기거나 legacy UUID endpoint를 제거할 때 |
| MD-2026-031 | 2026-08-17 | 점주 금융 화면의 권한 정합화와 운영 환불 form 제거 | (1) 정산 명세 조회(`GET /stores/{storeId}/settlements/{batchId}/items`)의 role set을 batch 목록과 같은 `OWNER` 전용으로 좁힌다. (2) 운영 콘솔의 UUID 환불 form(`OpsRefundPage`)과 `/ops/refunds` route를 제거한다. `POST /operations/payments/{paymentId}/refunds` API는 그대로 둔다 | (1) 명세는 주문별 수수료·혜택 원가·실지급액이라 영수증이 아니라 매장 원장이고, authorization matrix·target OpenAPI 설명·Plan 90 BR 2·이의제기 접수 권한이 모두 OWNER를 말한다. batch 목록이 OWNER 전용이라 STAFF가 `settlementBatchId`를 얻을 경로 자체가 없어 도달 불가능한 틈이었다. (2) 그 form은 운영자 Bearer 토큰으로 Merchant Chain 경로를 호출해 `ActorCredentialIsolationFilter`가 항상 403을 냈다. 즉 제거하기 전에도 동작하지 않았다. 점주 환불은 주문번호 기반 화면이 대체하고, 운영자 환불이 다시 필요해지면 Operations 경로로 별도 화면을 만든다 | `SettlementItemController.kt`, `SettlementItemQueryIntegrationTest.kt`, `frontend/src/pages/console/ConsolePages.tsx`, `frontend/src/router.tsx` | STAFF에게 정산 명세를 열어야 하는 실제 운영 요구가 생기거나, 운영자 환불 실행 화면이 P0으로 승격될 때 |
| MD-2026-032 | 2026-08-17 | 전체 test coverage를 유지하는 required CI 최적화 | PR scope를 `docs`, `frontend`, `backend`, `full`로 닫힌 분류하고 backend scope는 compiled Gradle test class 전체를 실행한다. main ruleset의 `build` context는 scope별 필수 job을 집계하는 최종 gate로 유지한다. test JVM당 PostGIS server를 하나만 시작하고 class별 database를 격리하며, 성공/실패 JUnit timing 증적으로 deterministic LPT shard를 구성한다. **개정 (2026-08-18):** hosted critical path 수용 기준은 동일 candidate SHA 세 run 각각 17분 이내로 한다. Storybook interaction은 required frontend gate에 유지하되 a11y는 기본 `error` 검사를 별도 advisory step으로 계속 실행하고, 그 위반만으로 required `build`를 실패시키지 않는다 | test 제거나 affected-context 선택은 금융·동시성·cross-context 회귀를 main 병합 후에야 발견할 위험이 있다. 반면 현재의 per-class container startup, backend/frontend 교차 실행, class-count shard는 coverage를 줄이지 않고도 제거할 수 있는 비용이다. malformed compare, unknown path, Docker·DB·timing 실패는 `full` 또는 failed gate로 닫혀 있다. 실제 6-shard run `32058946545`는 269개 class와 1,334개 test를 failure/error 없이 `15m15s`에 완료해 개정된 17분 기준 안에 들었다. 기존 색상 대비 한 건은 interaction·type·unit·build 실패가 아닌 접근성 후속 작업이므로 신호는 보존하되 병합과 분리한다 | `.github/workflows/ci.yml`, `build.gradle.kts`, `scripts/ci/`, `.storybook/preview.tsx`, test-only PostGIS runtime | backend full gate가 동일 조건 3회 측정에서 17분을 초과하거나, shard 편차가 25%를 넘거나, runner-minute 비용이 운영 상한을 넘을 때, 또는 a11y를 다시 required gate로 승격할 운영 근거가 생길 때 |
| MD-2026-033 | 2026-09-05 | 고객 주문 화면 개선의 인증 경계와 혜택 표시 | 사용자 결정에 따라 매장·메뉴 탐색을 포함한 현재 로그인 정책을 유지한다. 마이페이지 쿠폰은 기존 API의 필수 storeId 범위로 표시하며 전체 보유 수량으로 추정하지 않는다. 포인트는 기존 서버 견적과 주문 검증으로 적용한다 | 인증 경계나 혜택 정책 변경 없이 주문 화면의 가독성과 실제 조작 경로를 개선한다 | 고객 화면, customer-ordering-usability ExecPlan, canonical Storybook | 공개 탐색이나 매장과 무관한 전체 쿠폰 요약 API가 제품 요구로 승인될 때 |

## ID format

`MD-YYYY-NNN`
